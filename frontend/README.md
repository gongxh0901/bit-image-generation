# Frontend MVP

当前提供的是一个零依赖静态页面，便于快速联调后端 API。

## 使用方式

1. 先启动后端（见 `backend/README.md`）。
2. 本地打开本文件同目录下的 `index.html`。
3. 在页面中执行：
   - 创建风格
   - 启动训练
   - 提交生成
   - 查看任务列表
   - 观察 WebSocket 实时进度日志

> 后续如果你想切换到 React/Vue 正式工程，我可以基于这个功能直接迁移成 Vite + React 项目结构。
